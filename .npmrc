# Configuración para resolver conflictos de peer dependencies
legacy-peer-deps=true
auto-install-peers=true
# Asegurar que las dependencias opcionales se instalen (necesario para binarios nativos)
fund=false
audit=false

